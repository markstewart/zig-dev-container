# Try zig in VSCode using a dev container

## Prerequisites

1. Docker
2. VSCode
3. VSCode remote containters plugin: `ms-vscode-remote.remote-containers`

## Get Started

Open this repo using remote containers.

Inside the container:

```bash
zig init-exe 
```

```bash
zig build
```